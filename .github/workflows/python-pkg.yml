# This workflow will upload a Python Package using Twine when a release is created
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python#publishing-to-package-registries

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Python Package

on:
  release:
    types: [ published ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest ]

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[dev]
          playwright install chromium
        env:
          PLAYWRIGHT_BROWSERS_PATH: ./pw-browsers
      - name: Build package
        run: |
          pyinstaller bolletta_sync/app.py --onefile --add-data "pyproject.toml:." --add-data "pw-browsers:pw-browsers" --add-data "icon.ico:." --windowed --name bolletta-sync --icon icon.ico
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}
          path: dist/bolletta-sync*

  release:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Rename files for release
        run: |
          mv artifacts/build-ubuntu-latest/bolletta-sync bolletta-sync_linux
          mv artifacts/build-windows-latest/bolletta-sync.exe bolletta-sync_windows.exe
      - name: Update release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            bolletta-sync_linux
            bolletta-sync_windows.exe
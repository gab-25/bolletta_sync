version: "3.8"

services:
  bolletta-sync:
    build: .
    container_name: bolletta-sync
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./data:/app/data
